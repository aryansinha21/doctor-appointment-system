# Doctor Appointment Client

A simple React frontend for the Doctor Appointment System.

## Run locally

1. Open a terminal in `client`
2. Run `npm install`
3. Run `npm run dev`

The app will be available at `http://localhost:5173` and calls the backend at `http://localhost:5000/api`.
